[project]
name = "ctms-app"

[[services]]
name = "ctms-backend"
source = "backend"
builder = "NIXPACKS"

[services.variables]
JAVA_OPTS = "-Xmx512m"
PORT = "8080"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

